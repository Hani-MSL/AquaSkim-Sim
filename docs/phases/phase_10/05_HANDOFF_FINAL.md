# Final Handoff

The required final teaching deliverables are located at:

- Word report: `outputs/reports/AquaSkim-Sim_Final_Report.docx`
- Full package: `outputs/deliverables/AquaSkim-Sim_Submission.zip`
- Source code and evidence: project root folders `src/`, `config/`, `scripts/`, `tests/`, `outputs/`, and `records/`

The project is reproducible via `scripts/bootstrap_and_build.bat`.
