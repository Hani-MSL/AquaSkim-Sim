# Reference Visual Protocol

The protocol is declared in `config/reference_visualisation.yaml` and is not a
control input. It controls only replay sampling and encoding.

Each replay is sampled from the logged numerical rows generated by the fixed
reference design. State, controller regime, SOC, hopper volume, safety clearance
and route assignments shown in a replay originate from those rows/events.
