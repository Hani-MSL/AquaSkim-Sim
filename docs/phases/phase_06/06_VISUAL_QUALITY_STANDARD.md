# Phase 06 — Visual Quality Standard

Each Phase 06 figure is exported as both high-resolution PNG and SVG. Technical text is kept in dedicated panels rather than placed over geometry. Axes, legends and data traces use restrained print-oriented styling. The automated quality gate checks minimum resolution, SVG existence and non-trivial file size.
