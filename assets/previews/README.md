# Preview assets

This folder is reserved for lightweight images used in the README and documentation.

The full-resolution figures, GIFs and MP4 files are generated locally by:

```bat
scripts\run_from_zero_to_delivery.bat
```

Generated media are intentionally not committed to Git because they are reproducible from source.
