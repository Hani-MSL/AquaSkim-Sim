# CAD workspace

This folder is reserved for optional parametric CAD and conceptual geometry exports.

Generated CAD outputs should be written to `cad/generated/` and kept out of Git unless they are intentionally selected as lightweight preview assets.

The CAD model, when used, should be derived from `config/base_parameters.yaml` so geometry, mass properties and simulation assumptions stay aligned.
